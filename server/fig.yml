elasticsearch:
  image: dockerfile/elasticsearch
  command: /elasticsearch/bin/elasticsearch -Des.config=/data/elasticsearch.yml
  expose: "9200"
  volumes: custom-elasticsearch/data:/data

nginx:
  build: custom-nginx
  links: elasticsearch
  ports: "8080:8080"
